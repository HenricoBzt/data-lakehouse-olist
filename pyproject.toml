[tool.ruff]

line-length = 120

exclude = [".git", "__pycache__", ".github"]

extend-exclude = [
    "notebooks/setup/*",
    "notebooks/gold/*" 
]

builtins = ["spark", "dbutils", "display"]

[tool.ruff.lint]

select = ["E", "F"]


ignore = ["E501"]